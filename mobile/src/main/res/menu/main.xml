<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/start_action" android:showAsAction="ifRoom|withText" android:title="@string/start_text"
        tools:ignore="AppCompatResource" />
    <item android:id="@+id/cancel_action" android:showAsAction="ifRoom|withText" android:title="@string/cancel_text"
        tools:ignore="AppCompatResource" />
</menu>